import react from 'react';

class Reset extends react.Component {
    render(){
        return(
                <button type="button" className="btn btn-primary mt-3">Reset</button>
        )
    }
}

export default Reset